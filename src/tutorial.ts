// let prices: number[] = [100, 75, 42];
// prices.push('hello')

// let fruits: string[] = ['apple, orange'];

// let randomValues: [] = ['hello'];

// let emptyValues: number[] = [];

// let names = ['peter', 'susan', 1];
// let array: (string | boolean)[] = ['apple', true, 'apple', false];
// array.push(23);

// let temperatures: number[] = [23, 43, 64, 23, 45];
// // temperatures.push('temp');

// let colors: string[] = ['blue', 'red', 'black'];
// // colors.push(true);

// let mixedArray: (string | number)[] = [23, 'red', 45, 'blue'];
// // mixedArray.push(true);

// let car: { brand: string; year: number; model: string } = {
//   brand: 'honda',
//   year: 2020,
//   model: 'civic',
// };
// car.brand = 'ford';
// // car.color = 'blue';

// let car1: { brand: string; year: number; model: string } = {
//   brand: 'ford',
//   year: 2020,
//   model: 'raptor',
// };

// let car2: { brand: string; year: number; model: string } = {
//   brand: 'ford',
// };
// let book = { title: 'book', cost: 20 };
// let pen = { title: 'pen', cost: 5 };
// let notebook = { title: 'notebook' };

// let items: { readonly title: string; cost?: number }[] = [book, pen, notebook];
// items[0].title = 'new book';

let bike: { brand: string; year: number } = { brand: 'Trek', year: 2020 };
bike.year = 'Cannondale';

let laptop: { brand: string; year: number } = { brand: 'Lenovo' };

let product1 = { title: 'Tide', price: 25 };
let product2 = { title: 'Tide2' };

let arrayProducts: { title: string; price?: number }[] = [product1, product2];

arrayProducts.push({ title: 'Champion', price: '$12' });
